{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22",
    "bun": "1.3"
  },
  "steps": {
    "install": {
      "commands": ["bun install --frozen-lockfile"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["cd apps/web && bun run build"]
    }
  },
  "deploy": {
    "startCommand": "node apps/web/.output/server/index.mjs"
  }
}
